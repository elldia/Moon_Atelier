name: ebk
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  file_picker: ^12.2.0
  # Direct dep (otherwise only transitive via file_picker) so we can pass
  # FilePickerWebOptions(cancelUploadOnWindowBlur: false) — see
  # library_screen.dart's _pickBook() for why that's necessary on iOS.
  file_picker_web: ^3.1.0
  epub_view: ^3.3.0
  pdfx: ^2.11.0
  archive: ^4.0.9
  xml: ^6.6.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  uuid: ^4.6.0
  cp949_codec: ^1.0.2
  web: ^1.1.1
  # Native (Android/iOS/desktop) text-to-speech -- the web build keeps using
  # the browser's own Web Speech API instead (see utils/tts_reader_web.dart).
  flutter_tts: ^4.2.3
  google_fonts: ^6.2.1
  url_launcher: ^6.3.1
  http: ^1.2.2
  # Direct deps (otherwise only transitive via epub_view) so the EPUB
  # viewer's search/TTS can independently parse chapter HTML into plain
  # text — epub_view itself only exposes rendering, not raw chapter text.
  epubx: ^4.0.0
  html: ^0.15.6
  pdfrx: ^2.6.1
  webview_flutter: ^4.14.1
  flutter_markdown_plus: ^1.0.12
  share_plus: ^13.3.0
  ftpconnect: ^3.0.0
  shelf: ^1.4.2
  shelf_multipart: ^2.0.1

dependency_overrides:
  # flutter_html 3.0.0 (via epub_view) calls a private query_selector API
  # that html 0.15.7 removed; pin to the last compatible patch.
  html: 0.15.6
  # Vendored copy of pdfx 2.11.0 with one patch: PdfViewPinch hardcodes
  # wheel-scroll to pan-only with no public override, which blocks the
  # mouse-wheel PDF zoom feature. See vendor/pdfx/lib/src/viewer/pinch/pdf_view_pinch.dart.
  pdfx:
    path: vendor/pdfx
  # epubx (^3.0.8) pins an old `image` major that predates the API pdfrx's
  # native backend needs (ChannelOrder, Image.fromBytes(width, height,
  # bytes)). epubx only calls the long-stable decodeImage()/Image type, so
  # it's unaffected by the newer major.
  image: ^4.9.2
  # epubx (^4.0.0) also pins an old `archive` major; only calls the
  # long-stable Archive/ArchiveFile/.files/.name surface plus
  # ZipDecoder().decodeBytes(), all unchanged across the newer major.
  archive: ^4.0.9
  # Vendored copy of epubx 4.0.0 with one patch: book_cover_reader.dart
  # calls image's decodeImage() with a List<int>, which the bumped `image`
  # major above no longer accepts (Uint8List only). See
  # vendor/epubx/lib/src/readers/book_cover_reader.dart.
  epubx:
    path: vendor/epubx

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/icon/app_icon.png

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
  fonts:
    - family: Pretendard
      fonts:
        - asset: assets/fonts/Pretendard-Light.otf
          weight: 300
        - asset: assets/fonts/Pretendard-Regular.otf
          weight: 400
        - asset: assets/fonts/Pretendard-Medium.otf
          weight: 500
        - asset: assets/fonts/Pretendard-SemiBold.otf
          weight: 600
        - asset: assets/fonts/Pretendard-Bold.otf
          weight: 700
    - family: MaruBuri
      fonts:
        - asset: assets/fonts/MaruBuri-Light.ttf
          weight: 300
        - asset: assets/fonts/MaruBuri-Regular.ttf
          weight: 400
        - asset: assets/fonts/MaruBuri-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/MaruBuri-Bold.ttf
          weight: 700
